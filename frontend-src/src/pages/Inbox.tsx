import { Placeholder } from "../components/Placeholder";

export function Inbox() {
  return (
    <Placeholder
      title="Inbox"
      note="Unklassifizierte Einträge (enum 0) mit Draft-State + Apply/Cancel, Mehrfachauswahl, Filtern und Detailpanel folgen in PR 3."
    />
  );
}
